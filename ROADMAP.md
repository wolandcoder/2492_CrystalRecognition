# Дорожная карта проекта

> Проект АО «БЗПП». Отчёт понедельно.

---

## Общая схема по неделям

```mermaid
flowchart LR
    classDef now    fill:#fde68a,stroke:#b45309,color:#111,stroke-width:2px;
    classDef next   fill:#bfdbfe,stroke:#1d4ed8,color:#111,stroke-width:1px;
    classDef finish fill:#bbf7d0,stroke:#15803d,color:#111,stroke-width:1px;

    W1["Неделя 1 — пн 20 – вс 26 апреля — Mach3, зрение, камера"]:::now
    W2["Неделя 2 — пн 27 апреля – вс 3 мая — Манипулятор и мини-ПК"]:::next
    W3["Неделя 3 — пн 4 – вс 10 мая — Тестирование и сборка"]:::finish

    W1 --> W2 --> W3
```

---

## Неделя 1 — пн 20 – вс 26 апреля (текущая)

**Цель:** надёжный канал управления манипулятором и точная система координат.

```mermaid
flowchart TB
    classDef goal fill:#fde68a,stroke:#92400e,color:#111,stroke-width:2px;
    classDef task fill:#fef3c7,stroke:#b45309,color:#111;

    G1["Цель недели"]:::goal
    G1 --> T1["Mach3 — ограничения и интеграция с манипулятором"]:::task
    G1 --> T2["Алгоритмы защиты — лимиты осей и безопасные зоны"]:::task
    G1 --> T3["Привязка к новой системе координат"]:::task
    G1 --> T4["Совершенствование калибровки оптического зрения"]:::task
    G1 --> T5["Подбор новой камеры и сравнение с аналогами"]:::task
```

---

## Неделя 2 — пн 27 апреля – вс 3 мая

**Цель:** замкнуть контур манипулятора и собрать всё на одной машине.

```mermaid
flowchart TB
    classDef goal fill:#bfdbfe,stroke:#1e3a8a,color:#111,stroke-width:2px;
    classDef task fill:#dbeafe,stroke:#1d4ed8,color:#111;

    G2["Цель недели"]:::goal
    G2 --> M1["Завершение интеграции Mach3 и манипулятора"]:::task
    G2 --> M2["Имитация движения трубки с вакуумом — пока тросик"]:::task
    G2 --> M3["Алгоритм раскладки кристаллов по кассете"]:::task
    G2 --> M4["Подбор мини-ПК для встройки в манипулятор"]:::task
    G2 --> M5["Апгрейд серверной части — стабильность и удобный интерфейс"]:::task
    G2 --> M6["Перенос ПО на ПК у манипулятора — работа на одной машине"]:::task
```

---

## Неделя 3 — пн 4 – вс 10 мая

**Цель:** готовый прототип манипулятора.

```mermaid
flowchart TB
    classDef goal fill:#bbf7d0,stroke:#166534,color:#111,stroke-width:2px;
    classDef task fill:#dcfce7,stroke:#15803d,color:#111;

    G3["Цель недели"]:::goal
    G3 --> F1["Полевое тестирование"]:::task
    G3 --> F2["Правка найденных недочётов"]:::task
    G3 --> F3["Сборка манипулятора — новая камера, мини-ПК, серверное приложение"]:::task
    G3 --> F4["Финальный вид — компьютерное зрение и интерфейс оператора"]:::task
```

---

## Прогресс по статусам

```mermaid
flowchart LR
    classDef done fill:#bbf7d0,stroke:#15803d,color:#111;
    classDef now  fill:#fde68a,stroke:#b45309,color:#111,stroke-width:2px;
    classDef plan fill:#e5e7eb,stroke:#6b7280,color:#111;

    D1["GUI на Flet"]:::done
    D2["Pipeline нейро-модов"]:::done
    D3["Базовая работа с Mach3"]:::done
    D4["Калибровка масштаба из интерфейса"]:::done

    N1["Защита и ограничения для Mach3"]:::now
    N2["Новая система координат"]:::now
    N3["Подбор камеры"]:::now

    P1["Раскладка по кассете"]:::plan
    P2["Мини-ПК и перенос на одну машину"]:::plan
    P3["Полевое тестирование"]:::plan
    P4["Финальный прототип"]:::plan

    D1 --> N1
    D2 --> N2
    D3 --> N1
    D4 --> N2
    N1 --> P1
    N2 --> P2
    N3 --> P2
    P1 --> P3
    P2 --> P3
    P3 --> P4
```

Легенда: жёлтое — сейчас, зелёное — готово, серое — запланировано.
